[
	{
		"if": {
			"prefix": "if",
			"body": [
				"if (${1:condition}) {",
				"\t$0",
				"}"
			],
			"description": "Code snippet for an if statement"
		},
		"if/else": {
			"prefix": "if/else",
			"body": [
				"if (${1:condition}) {",
				"\t$0",
				"} else {",
				"\t",
				"}"
			],
			"description": "Code snippet for an if statement with else"
		},
		"for": {
			"prefix": "for",
			"body": [
				"for (\\$i = ${1:1}; ${2:\\$i <= 10}; \\$i${3:++}) {",
				"\t$0",
				"}"
			],
			"description": "Code snippet for a for loop"
		},
		"foreach": {
			"prefix": "foreach",
			"body": [
				"foreach (${1:condition} as \\$${2:key} => \\$${3:value}) {",
				"\t$0",
				"}"
			],
			"description": "Code snippet for a foreach loop"
		},
		"while": {
			"prefix": "while",
			"body": [
				"while (${1:condition}) {",
				"\t$0",
				"}"
			],
			"description": "Code snippet for a while loop"
		},
		"forever": {
			"prefix": "forever",
			"body": [
				"forever {",
				"\t$0",
				"}"
			],
			"description": "Code snippet for a forever loop"
		},
		"switch": {
			"prefix": "switch",
			"body": [
				"switch (${1:expression}) {",
				"\tcase ${2:expression}:",
				"\t\t${3:statments}",
				"\tbreak;",
				"\tdefault:",
				"\t\t${4:statments}",
				"\tbreak;",
				"}"
			],
			"description": "Code snippet for a switch statement"
		},
		"import": {
			"prefix": "import",
			"body": [
				"import { ${1:symbol} } from \"module.hsl\";"
			],
			"description": "Code snippet for an import statement"
		}
	}
]
